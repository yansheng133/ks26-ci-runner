apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ks26-app
spec:
  rules:
    - host: seat-CHANGE-ME.example-cluster.test
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ks26-app
                port: {number: 80}
